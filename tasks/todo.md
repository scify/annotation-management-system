# Task Plan

- [ ] Inspect the failing browser test setup and confirm why static analysis resolves `$this` to `PHPUnit\Framework\TestCase`.
- [ ] Apply the smallest structural fix so browser tests expose Laravel's `seed()` method to static analysis without suppressions.
- [ ] Validate the edited test files with targeted analysis and test execution.

## Review

- Pending.

